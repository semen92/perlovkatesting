use strict;
print('enter Y');
  my $Y=<STDIN>;
  print ('enter x'); 
  my $x=<STDIN>;
  print ('enter z');
  my $z=<STDIN>>
  sub maxandnin($y,$x,$z){
    maxv=undef
   our @arr=@_;
  for(@arr){
our $minv = $_ if !$minv || $_ < $minv;
$maxv = $_ if !$maxv || $_ > $maxv
  } 
  
  
 return $minv,$maxv
 }
 &maxandnin($y,$X,$Z);

 
 
 
 
